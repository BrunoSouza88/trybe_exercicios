let array = [9, 1, 2, 3, 9, 5, 7];


let qtdRepete = [];

  
  for (index = 0; index < array.length; index +=1) {
    for (indexSecudario = 1; index < array.length; indexSecudario += 1) {
        if (array[index] > arraySecudario[index]) {
            qtdRepete.push = (array[index])
        } console.log (qtdRepete.length)
    }
  } 
 
